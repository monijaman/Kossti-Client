
// Server Component
const Page = async () => {

  return (
    null
  );
}
// Note: `getServerSideProps` is not available in the `app/` directory, so we fetch the data directly here
export default Page;
